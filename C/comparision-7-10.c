#include <stdio.h>

void main()
{

    int a = 10;
    int b = 20;

    // Comparision operator / relational operator, it is used do comparision of both values and gives result in 1 or 0, where 1 means True and 0 means False.

    // printf("%d", a < b);
    // printf("%d", a > b);
    // printf("%d", a <= 10);
    // printf("%d", a >= 10);

    // equality operator, it checks if both values are same or not

    // printf("%d", a == 10);
    printf("%d", a != 20);
}