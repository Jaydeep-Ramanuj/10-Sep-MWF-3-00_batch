#include <stdio.h>

void main()
{

    int a = 10;

    // a = a + 1;
    // a += 1;

    // printf("\nBefore increment");
    // printf("\n%d", a);
    // // a++;
    // a--;
    // printf("\nAfter increment");
    // printf("\n%d", a);

    // pre / post

    // post increment / decrement, it first let use its value then does increment or decrement
    // pre increment / decrement, it does increment or decrement and let its value to be used

    // printf("\n%d", a++);
    // printf("\n%d", ++a);

    ++a;
    printf("\n%d", a);

    // a+=2;
}