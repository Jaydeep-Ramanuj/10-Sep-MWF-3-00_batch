#include <stdio.h>

void main()
{

    // Assignment operator (=), it is used assign value to a variable

    int a = 10;
    int b = 20;

    // a = b;

    // printf("%d", a);

    // shorthand notation

    // a = a + 10;

    // a += 10;
    // a -= 10;
    // a *= 10;
    // a /= 10;
    a %= 10;

    printf("%d", a);
}